#pragma once
#include "RendererUtilities.hpp"

class Mesh
{
	public:
    uint32_t vertexArray;
    uint32_t vertexBuffer;
    uint32_t indexBuffer;
    //texture;
    size_t indexCount;

	private:
	;

};